'use strict';

/**
 * internal-certificate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::internal-certificate.internal-certificate');
